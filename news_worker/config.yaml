news:
  # 拉取频率（秒）
  fetch_interval_seconds: 600
  # 每只股票最多拉取的新闻条数（单次执行）
  per_symbol_limit: 50
  # 关注的股票关键词列表（推荐使用股票中文名，提高新闻相关度）
  symbols:
    # - "浦发银行"   # 例如 600000
    # - "贵州茅台"   # 例如 600519
    - "600000"
    - "600519"

